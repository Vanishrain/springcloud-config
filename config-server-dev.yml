spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/ams?useSSL=false&useUnicode=true&characterEncoding=utf8
    username: postgres
    password: postgres
